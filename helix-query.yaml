autoGenerated: true
indices:
#All CONTENT#
  all-pages:
    include:
      - /**
    target: /all-pages.json
    properties:
      author:
        select: head > meta[name="author"]
        value: |
          attribute(el, "content")
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      type:
        select: head > meta[name="type"]
        value: |
          attribute(el, "content")
      tags:
        select: head > meta[name="tags"]
        value: |
          attribute(el, "content")

#Articles#
  articles:
    include:
      - /articles/**
    target: /articles-pages.json
    properties:
      author:
        select: head > meta[name="author"]
        value: |
          attribute(el, "content")
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      type:
        select: head > meta[name="type"]
        value: |
          attribute(el, "content")
      tags:
        select: head > meta[name="tags"]
        value: |
          attribute(el, "content")