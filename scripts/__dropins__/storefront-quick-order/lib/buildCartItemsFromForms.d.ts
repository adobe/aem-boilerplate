import { OrderItem } from '../types';

/**
 * Build cart items from forms for all order items
 * Only includes valid items (items with valid forms)
 */
export declare const buildCartItemsFromForms: (items: OrderItem[]) => (import('./buildCartItem').CartItem | null)[];
//# sourceMappingURL=buildCartItemsFromForms.d.ts.map