indices:
  mysite:
    include:
      - /**
    exclude:
    target: /query-index.json
    properties:
      title:
        select: select: head > meta[name="og:title"]
        value: |
          attribute(el, "content")
